package dev.starryeye.hello_coroutine.sub2_kotlin_coroutine

/**
 * Kotlin Coroutines
 *
 * 동시성을 지원하는 라이브러리이다.
 * 비동기 non-blocking 으로 동작하는 코드를 "동기 코드 처럼" 보이게한다.
 *
 * CoroutineContext 를 통해..
 *      dispatcher, error handling, threadLocal 등을 지원한다.
 *
 * CoroutineScope 를 통해..
 *      structured concurrency 와 cancellation 을 제공한다.
 *
 * flow, channel 등의 심화 기능을 제공하낟.
 */