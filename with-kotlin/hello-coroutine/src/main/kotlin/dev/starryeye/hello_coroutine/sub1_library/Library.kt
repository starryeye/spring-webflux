package dev.starryeye.hello_coroutine.sub1_library

/**
 * Kotlin 라이브러리
 *
 * Kotlin Coroutine
 *      kotlinx.coroutines 패키지로 지원한다.
 *      동시성과 관련하여 강력한 성능과 개발 편의를 제공한다.
 *
 * Kotlin Serialization
 *      kotlinx.serialization 패키지로 지원한다.
 *      객체의 serialization, deserialization 을 지원한다. (Jackson 처럼..)
 *
 * Kotlin Lincheck
 *      kotlinx.lincheck 패키지로 지원한다.
 *      동시성 버그를 탐지하고 디버그할 수 있게 테스트 환경을 제공한다.
 */